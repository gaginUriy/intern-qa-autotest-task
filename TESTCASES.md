Тест-кейсы для API микросервиса объявлений
Host: `https://qa-internship.avito.com`
Автор: Гагин ЮД
Дата создания: 08.04.2026
Версия API: v1, v2

TC-01: Создание объявления с валидными данными
| Атрибут | Значение |
| --- | --- |
| ID | TC-01 |
| Название | Создание объявления с валидными данными |
| Статус кейса | Активен |
| Подробное описание | Проверка успешного создания объявления при передаче всех обязательных полей с корректными значениями |
| Предусловие | Без предусловия |
| Шаги теста | 1. Отправить POST-запрос на `/api/1/item` с телом: <br> ```json <br> { <br>   "sellerID": 323300, <br>   "name": "Юрий", <br>   "price": 922337203685477580, <br>   "statistics": { <br>     "likes": -552, <br>     "viewCount": -4258, <br>     "contacts": -9563 <br>   } <br> } <br> ``` <br> 2. Проверить ответ сервера |
| Ожидаемый результат | Код ответа: 200. Тело ответа содержит сообщение: <br> ```json <br> { <br>   "status": "Сохранили объявление - <UUID>" <br> } <br> ``` <br> где UUID — уникальный идентификатор созданного объявления |
| Приоритет выполнения | P1 — Высокий |
| Автор | Гагин ЮД |

TC-02: Возврат уникального ID при создании объявления
| Атрибут | Значение |
| --- | --- |
| ID | TC-02 |
| Название | Возврат уникального ID при создании объявления |
| Статус кейса | Активен |
| Подробное описание | Проверка, что каждое созданное объявление получает уникальный UUID-идентификатор |
| Предусловие | Без предусловия |
| Шаги теста | 1. Отправить POST-запрос на `/api/1/item` с телом (вариант А): <br> ```json <br> { "sellerID": 323300, "name": "TestA", "price": 100 } <br> ``` <br> 2. Сохранить полученный ID_A. <br> 3. Отправить POST-запрос на `/api/1/item` с телом (вариант Б): <br> ```json <br> { "sellerID": 323300, "name": "TestB", "price": 100 } <br> ``` <br> 4. Сохранить полученный ID_B. <br> 5. Сравнить ID_A и ID_B. |
| Ожидаемый результат | Код ответа: 200 для обоих запросов. ID_A и ID_B являются валидными UUID и не совпадают между собой |
| Приоритет выполнения | P1 — Высокий |
| Автор | Гагин ЮД |

TC-03: Создание объявления с пустым полем name
| Атрибут | Значение |
| --- | --- |
| ID | TC-03 |
| Название | Создание объявления с пустым полем name |
| Статус кейса | Активен |
| Подробное описание | Негативный сценарий: проверка валидации обязательного поля name при передаче пустого значения |
| Предусловие | Без предусловия |
| Шаги теста | 1. Отправить POST-запрос на `/api/1/item` с телом: <br> ```json <br> { <br>   "sellerID": 323300, <br>   "name": "  ", <br>   "price": 1000 <br> } <br> ``` <br> 2. Проверить ответ сервера |
| Ожидаемый результат | Код ответа: 400. Тело ответа содержит: <br> ```json <br> { <br>   "result": { <br>     "message": "поле name обязательно", <br>     "messages": {} <br>   }, <br>   "status": "400" <br> } <br> ``` |
| Приоритет выполнения | P2 — Средний |
| Автор | Гагин ЮД |

TC-04: Создание объявления без обязательного поля price
| Атрибут | Значение |
| --- | --- |
| ID | TC-04 |
| Название | Создание объявления без обязательного поля price |
| Статус кейса | Активен |
| Подробное описание | Негативный сценарий: проверка обработки запроса с отсутствующим обязательным полем price |
| Предусловие | Без предусловия |
| Шаги теста | 1. Отправить POST-запрос на `/api/1/item` с телом (поле price отсутствует): <br> ```json <br> { <br>   "sellerID": 323300, <br>   "name": "Тест" <br> } <br> ``` <br> 2. Проверить ответ сервера |
| Ожидаемый результат | Код ответа: 400. Тело ответа содержит: <br> ```json <br> { <br>   "result": { <br>     "message": "поле price обязательно", <br>     "messages": {} <br>   }, <br>   "status": "400" <br> } <br> ``` |
| Приоритет выполнения | P2 — Средний |
| Автор | Гагин ЮД |

TC-05: Валидация поля name: запрещенные символы
| Атрибут | Значение |
| --- | --- |
| ID | TC-05 |
| Название | Валидация поля name: запрещенные символы |
| Статус кейса | Активен |
| Подробное описание | Проверка обработки поля name с запрещенными спецсимволами (например, `<`, `>`, `&`) |
| Предусловие | Без предусловия |
| Шаги теста | 1. Отправить POST-запрос на `/api/1/item` с телом, где `name` содержит запрещенный символ: <br> ```json <br> { <br>   "sellerID": 323300, <br>   "name": "Test<script>", <br>   "price": 100 <br> } <br> ``` <br> 2. Проверить ответ сервера |
| Ожидаемый результат | Код ответа: 400. Тело ответа содержит ошибку с указанием запрещенного символа, например: <br> ```json <br> { <br>   "result": { <br>     "message": "Символ < запрещён", <br>     "messages": {} <br>   }, <br>   "status": "400" <br> } <br> ``` |
| Приоритет выполнения | P2 — Средний |
| Автор | Гагин ЮД |

TC-05.1: Валидация поля name: минимальная длина (1 символ)
| Атрибут | Значение |
| --- | --- |
| ID | TC-05.1 |
| Название | Валидация поля name: минимальная длина (1 символ) |
| Статус кейса | Активен |
| Подробное описание | Негативный сценарий: проверка валидации поля name при передаче строки длиной 1 символ (если бизнес-логика требует минимальной длины > 1) |
| Предусловие | Без предусловия |
| Шаги теста | 1. Отправить POST-запрос на `/api/1/item` с телом: <br> ```json <br> { <br>   "sellerID": 323300, <br>   "name": "A", <br>   "price": 100 <br> } <br> ``` <br> 2. Проверить ответ сервера |
| Ожидаемый результат | Код ответа: 400. Тело ответа содержит сообщение об ошибке валидации длины, например: <br> ```json <br> { <br>   "result": { <br>     "message": "Минимальная длина поля name должна быть больше 1 символа", <br>     "messages": {} <br>   }, <br>   "status": "400" <br> } <br> ``` |
| Приоритет выполнения | P2 — Средний |
| Автор | Гагин ЮД |

TC-06: Обработка запроса с пустым телом или невалидным JSON
| Атрибут | Значение |
| --- | --- |
| ID | TC-06 |
| Название | Обработка запроса с пустым телом или невалидным JSON |
| Статус кейса | Активен |
| Подробное описание | Проверка реакции сервера на запросы с нарушенной структурой тела: пустое тело, невалидный JSON |
| Предусловие | Без предусловия |
| Шаги теста | 1. Отправить POST-запрос на `/api/1/item` с заголовком `Content-Type: application/json` и пустым телом `{}`. <br> 2. Отправить POST-запрос на `/api/1/item` с заголовком `Content-Type: application/json` и невалидным JSON: `{ "sellerID": 323300, "name": "test", }` <br> 3. Проверить ответ сервера для каждого случая |
| Ожидаемый результат | Код ответа: 400. Тело ответа содержит сообщение об ошибке валидации или синтаксической ошибке JSON (например: `{ "result": { "message": "не передано тело объявления" ... } }`) |
| Приоритет выполнения | P2 — Средний |
| Автор | Гагин ЮД |

TC-07: Получение существующего объявления по валидному UUID
| Атрибут | Значение |
| --- | --- |
| ID | TC-07 |
| Название | Получение существующего объявления по валидному UUID |
| Статус кейса | Активен |
| Подробное описание | Позитивный сценарий: проверка успешного получения ранее созданного объявления по его UUID |
| Предусловие | 1. Отправить POST-запрос на `/api/1/item` с телом: <br> ```json <br> { "sellerID": 323300, "name": "GetTest", "price": 500 } <br> ``` <br> 2. Сохранить полученный UUID из ответа. |
| Шаги теста | 1. Отправить GET-запрос на `/api/1/item/<UUID>`. <br> 2. Проверить тело ответа |
| Ожидаемый результат | Код ответа: 200. Тело ответа — массив с одним объектом, содержащим поля: `id`, `sellerId`, `name`, `price`, `statistics` (likes, viewCount, contacts), `createdAt`. Значения полей соответствуют данным, переданным при создании |
| Приоритет выполнения | P1 — Высокий |
| Автор | Гагин ЮД |

TC-08: Получение объявления по несуществующему UUID
| Атрибут | Значение |
| --- | --- |
| ID | TC-08 |
| Название | Получение объявления по несуществующему UUID |
| Статус кейса | Активен |
| Подробное описание | Негативный сценарий: проверка обработки запроса к ресурсу, который не существует в системе |
| Предусловие | Без предусловия |
| Шаги теста | 1. Отправить GET-запрос на `/api/1/item/<несуществующий-UUID>`. <br> 2. Проверить ответ сервера |
| Ожидаемый результат | Код ответа: 404. Тело ответа содержит: <br> ```json <br> { <br>   "result": { <br>     "message": "item <UUID> not found", <br>     "messages": null <br>   }, <br>   "status": "404" <br> } <br> ``` |
| Приоритет выполнения | P2 — Средний |
| Автор | Гагин ЮД |

TC-09: Получение всех объявлений по валидному sellerID
| Атрибут | Значение |
| --- | --- |
| ID | TC-09 |
| Название | Получение всех объявлений по валидному sellerID |
| Статус кейса | Активен |
| Подробное описание | Проверка успешного получения списка объявлений для продавца с существующими записями в системе |
| Предусловие | 1. Отправить POST-запрос на `/api/1/item` с телом: <br> ```json <br> { "sellerID": 323300, "name": "Item1", "price": 100 } <br> ``` <br> 2. Отправить POST-запрос на `/api/1/item` с телом: <br> ```json <br> { "sellerID": 323300, "name": "Item2", "price": 200 } <br> ``` |
| Шаги теста | 1. Отправить GET-запрос на `/api/1/323300/item`. <br> 2. Проверить структуру и содержимое ответа |
| Ожидаемый результат | Код ответа: 200. Тело ответа — массив объектов. Количество элементов >= 2. В каждом объекте поле `sellerId` равно `323300` |
| Приоритет выполнения | P1 — Высокий |
| Автор | Гагин ЮД |

TC-10: Получение списка для sellerID без объявлений
| Атрибут | Значение |
| --- | --- |
| ID | TC-10 |
| Название | Получение списка для sellerID без объявлений |
| Статус кейса | Активен |
| Подробное описание | Проверка обработки запроса к продавцу, у которого в системе нет ни одного объявления |
| Предусловие | Без предусловия (используется заведомо свободный sellerID, например, 999888) |
| Шаги теста | 1. Отправить GET-запрос на `/api/1/999888/item`. <br> 2. Проверить тело ответа |
| Ожидаемый результат | Код ответа: 200. Тело ответа — пустой массив: `[]` |
| Приоритет выполнения | P2 — Средний |
| Автор | Гагин ЮД |

TC-11: Получение списка по некорректному sellerID
| Атрибут | Значение |
| --- | --- |
| ID | TC-11 |
| Название | Получение списка по некорректному sellerID |
| Статус кейса | Активен |
| Подробное описание | Проверка валидации параметра sellerID: передача значения, которое не соответствует ожидаемому формату (целое число) |
| Предусловие | Без предусловия |
| Шаги теста | 1. Отправить GET-запрос на `/api/1/invalid-uuid-format/item`. <br> 2. Проверить ответ сервера |
| Ожидаемый результат | Код ответа: 400. Тело ответа содержит: <br> ```json <br> { <br>   "result": { <br>     "message": "передан некорректный идентификатор продавца", <br>     "messages": {} <br>   }, <br>   "status": "400" <br> } <br> ``` |
| Приоритет выполнения | P2 — Средний |
| Автор | Гагин ЮД |

TC-12: Проверка типа возвращаемого значения — массив объектов
| Атрибут | Значение |
| --- | --- |
| ID | TC-12 |
| Название | Проверка типа возвращаемого значения — массив объектов |
| Статус кейса | Активен |
| Подробное описание | Проверка, что эндпоинт возвращает именно массив, даже если найден один элемент, а не одиночный объект |
| Предусловие | 1. Отправить POST-запрос на `/api/1/item` с телом: <br> ```json <br> { "sellerID": 323301, "name": "SingleItem", "price": 100 } <br> ``` |
| Шаги теста | 1. Отправить GET-запрос на `/api/1/323301/item`. <br> 2. Проверить, что корневой элемент ответа — массив (`[...]`), а не объект (`{...}`) |
| Ожидаемый результат | Код ответа: 200. Тело ответа — массив, содержащий один объект с данными объявления. Структура каждого элемента соответствует ожидаемой схеме |
| Приоритет выполнения | P2 — Средний |
| Автор | Гагин ЮД |

TC-13: Получение статистики по существующему item ID
| Атрибут | Значение |
| --- | --- |
| ID | TC-13 |
| Название | Получение статистики по существующему item ID |
| Статус кейса | Активен |
| Подробное описание | Проверка успешного получения статистических данных для валидного существующего объявления |
| Предусловие | 1. Отправить POST-запрос на `/api/1/item` с телом: <br> ```json <br> { "sellerID": 323302, "name": "StatTest", "price": 100 } <br> ``` <br> 2. Сохранить полученный UUID. |
| Шаги теста | 1. Отправить GET-запрос на `/api/1/statistic/<UUID>`. <br> 2. Проверить тело ответа |
| Ожидаемый результат | Код ответа: 200. Тело ответа — массив с объектом, содержащим поля: `likes`, `viewCount`, `contacts`. Типы значений — числа |
| Приоритет выполнения | P1 — Высокий |
| Автор | Гагин ЮД |

TC-14: Получение статистики по несуществующему ID
| Атрибут | Значение |
| --- | --- |
| ID | TC-14 |
| Название | Получение статистики по несуществующему ID |
| Статус кейса | Активен |
| Подробное описание | Негативный сценарий: проверка обработки запроса статистики для несуществующего объявления |
| Предусловие | Без предусловия |
| Шаги теста | 1. Отправить GET-запрос на `/api/1/statistic/<несуществующий-UUID>`. <br> 2. Проверить ответ сервера |
| Ожидаемый результат | Код ответа: 404. Тело ответа содержит: <br> ```json <br> { <br>   "result": { <br>     "message": "statistic <UUID> not found", <br>     "messages": null <br>   }, <br>   "status": "404" <br> } <br> ``` |
| Приоритет выполнения | P2 — Средний |
| Автор | Гагин ЮД |

TC-15: Валидация формата ID в запросе статистики
| Атрибут | Значение |
| --- | --- |
| ID | TC-15 |
| Название | Валидация формата ID в запросе статистики |
| Статус кейса | Активен |
| Подробное описание | Проверка обработки некорректного формата UUID в параметре пути эндпоинта статистики |
| Предусловие | Без предусловия |
| Шаги теста | 1. Отправить GET-запрос на `/api/1/statistic/invalid-uuid-format`. <br> 2. Проверить ответ сервера |
| Ожидаемый результат | Код ответа: 400. Тело ответа содержит: <br> ```json <br> { <br>   "result": { <br>     "message": "передан некорректный идентификатор объявления", <br>     "messages": {} <br>   }, <br>   "status": "400" <br> } <br> ``` |
| Приоритет выполнения | P2 — Средний |
| Автор | Гагин ЮД |

TC-16: Актуальность статистики после взаимодействий
| Атрибут | Значение |
| --- | --- |
| ID | TC-16 |
| Название | Актуальность статистики после взаимодействий |
| Статус кейса | Активен |
| Подробное описание | Проверка, что статистика отражает изменения (например, увеличение viewCount) после действий пользователя |
| Предусловие | 1. Отправить POST-запрос на `/api/1/item` с телом: <br> ```json <br> { "sellerID": 323303, "name": "StatChange", "price": 100 } <br> ``` <br> 2. Сохранить UUID. <br> 3. Получить начальную статистику через GET `/api/1/statistic/<UUID>`. |
| Шаги теста | 1. Сымитировать взаимодействие (например, несколько раз выполнить просмотр объявления через GET `/api/1/item/<UUID>`). <br> 2. Повторно запросить статистику через `/api/1/statistic/<UUID>`. |
| Ожидаемый результат | Код ответа: 200. Значение `viewCount` во втором ответе больше, чем в первом. Поля `likes` и `contacts` также могут изменяться в зависимости от логики сервиса |
| Приоритет выполнения | P3 — Низкий |
| Автор | Гагин ЮД |

TC-17: Успешное удаление существующего объявления
| Атрибут | Значение |
| --- | --- |
| ID | TC-17 |
| Название | Успешное удаление существующего объявления |
| Статус кейса | Активен |
| Подробное описание | Проверка корректного удаления объявления по валидному UUID через DELETE-запрос |
| Предусловие | 1. Отправить POST-запрос на `/api/1/item` с телом: <br> ```json <br> { "sellerID": 323304, "name": "ToDelete", "price": 100 } <br> ``` <br> 2. Сохранить полученный UUID. |
| Шаги теста | 1. Отправить DELETE-запрос на `/api/2/item/<UUID>`. <br> 2. Проверить код ответа |
| Ожидаемый результат | Код ответа: 200 (или 204). Ресурс успешно удалён |
| Приоритет выполнения | P1 — Высокий |
| Автор | Гагин ЮД |

TC-18: Удаление несуществующего объявления
| Атрибут | Значение |
| --- | --- |
| ID | TC-18 |
| Название | Удаление несуществующего объявления |
| Статус кейса | Активен |
| Подробное описание | Негативный сценарий: попытка удаления ресурса, который отсутствует в системе |
| Предусловие | Без предусловия |
| Шаги теста | 1. Отправить DELETE-запрос на `/api/2/item/<несуществующий-UUID>`. <br> 2. Проверить ответ сервера |
| Ожидаемый результат | Код ответа: 404. Тело ответа содержит сообщение о том, что ресурс не найден |
| Приоритет выполнения | P2 — Средний |
| Автор | Гагин ЮД |

TC-19: Удаление по невалидному ID
| Атрибут | Значение |
| --- | --- |
| ID | TC-19 |
| Название | Удаление по невалидному ID |
| Статус кейса | Активен |
| Подробное описание | Проверка валидации формата UUID в параметре пути DELETE-запроса |
| Предусловие | Без предусловия |
| Шаги теста | 1. Отправить DELETE-запрос на `/api/2/item/not-a-uuid`. <br> 2. Проверить ответ сервера |
| Ожидаемый результат | Код ответа: 400. Тело ответа содержит: <br> ```json <br> { <br>   "result": { <br>     "message": "переданный id айтема некорректный", <br>     "messages": {} <br>   }, <br>   "status": "400" <br> } <br> ``` |
| Приоритет выполнения | P2 — Средний |
| Автор | Гагин ЮД |

TC-20: Проверка недоступности объявления после удаления
| Атрибут | Значение |
| --- | --- |
| ID | TC-20 |
| Название | Проверка недоступности объявления после удаления |
| Статус кейса | Активен |
| Подробное описание | Проверка, что после успешного DELETE-запроса объявление больше не доступно через GET-эндпоинт |
| Предусловие | 1. Отправить POST-запрос на `/api/1/item` с телом: <br> ```json <br> { "sellerID": 323305, "name": "DelCheck", "price": 100 } <br> ``` <br> 2. Сохранить UUID. <br> 3. Отправить DELETE-запрос на `/api/2/item/<UUID>`. |
| Шаги теста | 1. Отправить GET-запрос на `/api/1/item/<UUID>` для проверки доступности. |
| Ожидаемый результат | Код ответа: 404, тело содержит сообщение `"item ... not found"` |
| Приоритет выполнения | P1 — Высокий |
| Автор | Гагин ЮД |

TC-21: Идемпотентность операции удаления
| Атрибут | Значение |
| --- | --- |
| ID | TC-21 |
| Название | Идемпотентность операции удаления |
| Статус кейса | Активен |
| Подробное описание | Проверка, что повторный DELETE-запрос на уже удалённый ресурс возвращает предсказуемый ответ |
| Предусловие | 1. Отправить POST-запрос на `/api/1/item` с телом: <br> ```json <br> { "sellerID": 323306, "name": "Idempotent", "price": 100 } <br> ``` <br> 2. Сохранить UUID. <br> 3. Отправить первый DELETE-запрос на `/api/2/item/<UUID>`. |
| Шаги теста | 1. Повторно отправить DELETE-запрос на тот же UUID (второй DELETE-запрос). <br> 2. Проверить ответ сервера на второй запрос |
| Ожидаемый результат | Код ответа: 204. Отсутствие кода 500 и валидная структура ответа (или его отсутствие при 204) |
| Приоритет выполнения | P2 — Средний |
| Автор | Гагин ЮД |

TC-22: Недоступность статистики после удаления объявления
| Атрибут | Значение |
| --- | --- |
| ID | TC-22 |
| Название | Недоступность статистики после удаления объявления |
| Статус кейса | Активен |
| Подробное описание | Проверка, что после удаления объявления статистика по нему становится недоступной |
| Предусловие | 1. Отправить POST-запрос на `/api/1/item` с телом: <br> ```json <br> { "sellerID": 323307, "name": "StatDel", "price": 100 } <br> ``` <br> 2. Сохранить UUID. <br> 3. Отправить DELETE-запрос на `/api/2/item/<UUID>`. |
| Шаги теста | 1. Отправить GET-запрос на `/api/1/statistic/<UUID>`. <br> 2. Проверить ответ сервера |
| Ожидаемый результат | Код ответа: 404. Тело ответа содержит такое же сообщение, как и при запросе статистики несуществующего ID: <br> ```json <br> { <br>   "result": { <br>     "message": "statistic <UUID> not found", <br>     "messages": null <br>   }, <br>   "status": "404" <br> } <br> ``` |
| Приоритет выполнения | P2 — Средний |
| Автор | Гагин ЮД |